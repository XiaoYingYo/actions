{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "node-terminal",
            "name": "Push",
            "cwd": "${workspaceRoot}",
            "command": "node --version",
            "preLaunchTask": "Push"
        },
    ]
}