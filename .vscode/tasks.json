{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Push",
            "type": "shell",
            "options": {
                "cwd": "${workspaceFolder}"
            },
            "command": [
                "start push.bat"
            ]
        }
    ]
}